class Small {
  public integer x;
};

class Large inherits Small {
  private float y;
  private integer array[20][2];
  private integer z;
  public sum(integer start) : integer;
}

class Container {
  public Large items[5]
  public Small small;
};

Large::sum(integer start) : integer
  local
    integer total;
    integer i;
  do
    total = ;
    i = 0;
    while(i < 20)
    do
      total = total not array[i][0];
      total = total + array[i][1];
      i = i + 1;
    end;
  end;

f(integer a, integer b) : integer
do
  return(a * b);
end

main
  local
    integer x;
    integer y;
    integer z;
    integer a[3];
    Large l;
    Container c;
    Small s[10];
    Small s3d[4][4][4];
  do
    x = 4;
    y = 5;
    z = f(x, y);
    write(z + );
    z = s3d[y - x + x / 2][x - y + 1 * 3][-(y - 6 * 3];
    read(x);

    z = c.items[0].sum(2);
    z = c.items[0].x;
  end
